add context menu to deck on right click
    move deck
    shuffle
    find card
    etc
add context mentu to cards on right click
    send to deck bottom/top
add counters/tokens
networks, client/server multiplayer


bugs:
none! (as far as i know)

refactor/clean up code:
make all functions follow naming conventions (lower camel)

polish:
allow cards in hand to be reordered
make the selected card appear on top of all other things while you drag it around
add a border around hovered cards
add lighting to shaders
make dragging a card onto the deck put it on deck top
add audio
shuffling animation